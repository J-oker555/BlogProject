{{ form_start(form) }}
    {{ form_widget(form) }}
    <div class="row mt-4">
        <div class="col-md-6">
            <button type="submit" class="btn btn-primary">{{ button_label }}</button>
        </div>
    </div>

{{ form_end(form) }}
